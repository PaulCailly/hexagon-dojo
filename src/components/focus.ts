// Shared visible-focus ring for keyboard navigation (kept as one static
// string so the Tailwind compiler sees every class).
export const focusRing =
  "focus-visible:outline focus-visible:outline-2 focus-visible:outline-cyan-400";
